#[allow(unused_variables)]

// Main es punto de entrada
fn main() {
  // Macro
  // Cuando una funcion termina con un simbolo de exclamacion !, eso es una macro
  println!("Hola mundo");

  let v: Vec<i32> = vec![1, 2, 3, 4];
  println!("El vetor es {:?}", v);
}